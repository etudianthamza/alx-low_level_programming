#include "main.h"

/**
 * main - the function of getting the name
 * @argc : the integer
 * @argv : the character
 *
 * Return: Always 0 (Success)
*/

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
