#include "main.h"

/**
 * swap_int - swaps the values of two integers
 *
 * @a : a number to swip
 *
 * @b : a number to swip
 *
 * Return: Always 0 (Success)
*/

void swap_int(int *a, int *b)
{
	int *c;

	&a = &b;
	&c = &a;
	&b = &c;
	return (0);
}
