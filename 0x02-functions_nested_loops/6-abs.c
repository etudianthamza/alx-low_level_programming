#include "main.h"
#include <math.h>

/**
 * _abs - write the alphabets in lowercase
 *
 * @i : the alphabets to print
 *
 * Return: on success 1.
*/

int _abs(int i)
{
	int r;

	r = sqrt(i * i);
	return (r);
}
